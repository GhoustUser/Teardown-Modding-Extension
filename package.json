{
	"name": "teardown-modding-extension",
	"displayName": "Teardown Modding Extension",
	"description": "Adds intellisense for Teardown's scripting API.",
	"author": {
		"name": "GhoustUser",
		"url": "https://github.com/GhoustUser"
	},
	"version": "0.5.1",
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"package": "vsce package"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/GhoustUser/Teardown-Modding-Extension"
	},
	"keywords": [
		"teardown",
		"modding",
		"lua",
		"api"
	],
	"icon": "./icon.png",
	"engines": {
		"vscode": "^1.108.1"
	},
	"activationEvents": [
		"workspaceContains:info.txt"
	],
	"contributes": {
		"configuration": {
			"title": "Teardown Modding Extension",
			"properties": {
				"teardownModding.enableIntellisense": {
					"type": "boolean",
					"default": false,
					"title": "Enable Intellisense",
					"description": "Enables the Teardown scripting API intellisense. (workspace only)"
				},
				"teardownModding.showPrompt": {
					"type": "boolean",
					"default": true,
					"description": "Show prompt to enable Teardown scripting API intellisense when info.txt is detected. (once per workspace)"
				}
			}
		}
	},
	"devDependencies": {
		"@types/node": "^25.0.8",
		"@types/vscode": "^1.108.1"
	}
}
