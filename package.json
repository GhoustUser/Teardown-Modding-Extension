{
	"name": "teardown-modding-extension",
	"displayName": "Teardown Modding Extension",
	"description": "A VS Code extension to make modding Teardown easier, with Lua API references and mod management.",
	"author": {
		"name": "GhoustUser",
		"url": "https://github.com/GhoustUser"
	},
	"version": "0.3.0",
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"package": "vsce package"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/GhoustUser/Teardown-Modding-Extension"
	},
	"keywords": [
		"teardown",
		"modding",
		"lua",
		"api"
	],
	"icon": "./icon.png",
	"engines": {
		"vscode": "^1.108.1"
	},
	"activationEvents": [
		"workspaceContains:info.txt"
	],
	"contributes": {
		"configuration": {
			"title": "Teardown Modding Extension configuration",
			"properties": {
				"teardownModding.openModViewByDefault": {
					"type": "boolean",
					"default": false,
					"description": "Whether to open the Mod View automatically when the extension is activated."
				}
			}
		},
		"commands": [
			{
				"command": "teardownModding.openModView",
				"title": "Open Teardown Mod View"
			}
		]
	},
	"devDependencies": {
		"@types/node": "^25.0.8",
		"@types/vscode": "^1.108.1"
	}
}
