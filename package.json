{
	"name": "teardown-lua-api",
	"displayName": "Teardown lua API",
	"description": "Adds Teardown's Lua API to the Lua language server for better autocompletion and documentation support.",
	"author": {
		"name": "GhoustUser",
		"url": "https://gitlab.com/GhoustUser"
	},
	"version": "0.1.0",
	"main": "./extension.js",
	"keywords": [
		"teardown",
		"modding",
		"lua",
		"api"
	],
	"icon": "./icon.png",
	"engines": {
		"vscode": "^1.85.0"
	},
	"activationEvents": [
		"workspaceContains:info.txt"
	],
	"contributes": {
		"configuration": {
			"title": "Teardown Modding Extension configuration",
			"properties": {
				"TeardownModding.hasPrompted": {
					"type": "boolean",
					"default": false,
					"description": "Indicates whether the user has been prompted about enabling the Teardown Modding Extension features."
				},
				"TeardownModding.enabled": {
					"type": "boolean",
					"default": false,
					"description": "Enable or disable the Teardown Modding Extension features."
				}
			}
		},
		"customEditors": [
			{
				"viewType": "CustomEditor-info_txt",
				"displayName": "Custom Info Editor",
				"selector": [
					{
						"filenamePattern": "info.txt"
					}
				]
			}
		]
	}
}